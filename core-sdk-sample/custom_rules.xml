<?xml version="1.0" encoding="UTF-8"?>
<project name="levelup-core-sample-custom" default="help">
    <property name="dist_package_name" value="LevelUp-Android-SDK-Sample" />

    <include file="../whitelabel-android-sdk/levelup-core-lib/git_rules.xml" />
    <include file="../whitelabel-android-sdk/levelup-core-lib/levelup_dist.xml" />

    <target name="-pre-dist-source" depends="git.revision">
        <property name="dist_package_version" value="${repository.version}" />
        <property name="dist_package" value="${dist_package_name}-${dist_package_version}" />
    </target>

    <target name="dist-source" description="Create a distribution package of the source"
        depends="-pre-dist-source, clean, levelup-dist.dist-source" />
</project>

